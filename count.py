#prompting the user for an input
hours = input('Enter Hours: ')
rate = input('Enter Rate: ')
#calculatitng the hours and rating
pay = float(hours) * float(rate)
#printing the amount that they have to pay
print("Pay:", pay)